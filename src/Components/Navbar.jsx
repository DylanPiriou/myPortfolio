import React from 'react'

export default function Navbar() {
  return (
    <div className="navbar">
        <span>@dylanpiriou/</span>
        <nav>
            <a href="#">HOME</a>
            <a href="#">PROJECTS</a>
            <a href="#">CONTACT</a>
        </nav>
    </div>
  )
}
